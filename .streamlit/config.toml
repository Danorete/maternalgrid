[server]
headless = true

[browser]
gatherUsageStats = false

# Base colours. Keep these in step with the tokens at the top of
# assets/theme.css, which controls everything else.
[theme]
primaryColor = "#256abf"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f7f8fa"
textColor = "#16202c"
font = "sans serif"
